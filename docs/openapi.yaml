openapi: 3.0.0

info:
  title: Контактна Книга
  version: 1.0.0
  description: >
    Це API для керування контактами. Дозволяє створювати, оновлювати, видаляти та переглядати контакти.
  license:
    name: Apache 2.0
    url: http://www.apache.org/licenses/LICENSE-2.0.html

tags:
  - name: Contacts
    description: Операції з контактами

servers:
  - url: https://nodejs-hw-mongodb-1m6g.onrender.com
    description: Deployed server
  - url: http://localhost:3000
    description: Local server

paths:
  /contacts:
    get:
      tags:
        - Contacts
      $ref: './paths/contacts/get.yaml'
    post:
      tags:
        - Contacts
      $ref: './paths/contacts/post.yaml'

  /contacts/{contactId}:
    get:
      tags:
        - Contacts
      $ref: './paths/contacts/{id}/get.yaml'
    patch:
      tags:
        - Contacts
      $ref: './paths/contacts/{id}/patch.yaml'
    delete:
      tags:
        - Contacts
      $ref: './paths/contacts/{id}/delete.yaml'

components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT

  schemas:
    Contact:
      $ref: './components/schemas/Contact.yaml'

  responses:
    NotFound:
      $ref: './components/responses/NotFound.yaml'
